# Docker Production Environment
# These values are used when building for Docker deployment

# API is proxied through Nginx, so use relative paths
VITE_API_BASE_URL=
VITE_WS_BASE_URL=

# Application Title
VITE_APP_TITLE=GGP Poker Video Catalog

# Disable debug mode in production
VITE_DEBUG=false
